'use client';
import { useState } from 'react';
import { Activity, Bell, ChevronRight, Crosshair, Shield, Users, WalletCards } from 'lucide-react';
const roles = { SUPER_ADMIN: { label: 'SUPER ADMIN', title: 'Command center', stats: [['ACTIVE OPS', '08'], ['PENDING KYC', '12'], ['PAYOUT QUEUE', '04']], actions: ['Tournament control', 'Match verification', 'Wallet & payouts'] }, WORKER: { label: 'WORKER', title: 'Operations desk', stats: [['LIVE MATCHES', '03'], ['RESULTS TO REVIEW', '17'], ['OPEN DISPUTES', '06']], actions: ['Tournament control', 'Match verification', 'KYC queue'] }, CAPTAIN: { label: 'CAPTAIN', title: 'Team command', stats: [['ROSTER', '05 / 05'], ['OPEN REGISTRATIONS', '03'], ['RESULTS DUE', '02']], actions: ['Manage roster', 'Register team', 'Submit proof'] }, PLAYER: { label: 'PLAYER', title: 'Player dashboard', stats: [['TOURNAMENTS', '12'], ['TEAM STATUS', 'READY'], ['WALLET', '₹ 2,450']], actions: ['Browse tournaments', 'My team', 'Notifications'] } };
export default function Home() { const [role, setRole] = useState('SUPER_ADMIN'); const panel = roles[role]; return <main className="shell"><header className="topbar"><div className="brand"><Crosshair size={20} /><span>APEX<span className="ember">NOVA</span></span></div><div className="status"><span className="pulse" /> SYSTEM ONLINE <span className="divider" /> MOCK ENVIRONMENT</div><button className="iconButton" aria-label="Notifications"><Bell size={18} /></button></header><section className="hero"><div><p className="eyebrow">APEXFLOW / OPERATIONS CONSOLE</p><h1>{panel.title}</h1><p className="lede">Competitive intelligence for the next match. Track every registration, result, and decision from one field-ready view.</p></div><div className="heroMark"><Shield size={42} /><span>NO. 001</span></div></section><nav className="roleTabs">{Object.entries(roles).map(([key, value]) => <button className={role === key ? 'active' : ''} onClick={() => setRole(key)} key={key}>{value.label}</button>)}</nav><section className="stats">{panel.stats.map(([label, value]) => <div className="stat" key={label}><span>{label}</span><strong>{value}</strong><small>UPDATED JUST NOW</small></div>)}</section><section className="workspace"><div className="sectionHeading"><div><p className="eyebrow">ACTIVE MODULES</p><h2>Operational overview</h2></div><span className="mono">03 / 09 ENABLED</span></div><div className="moduleGrid">{panel.actions.map((action, index) => <article className="module" key={action}><div className="moduleIcon">{index === 0 ? <Activity /> : index === 1 ? <Crosshair /> : <Users />}</div><div><p className="moduleIndex">0{index + 1} // READY</p><h3>{action}</h3><p>{role === 'PLAYER' && index === 2 ? 'Keep every update within reach.' : 'Review queues, status, and next actions.'}</p></div><ChevronRight className="arrow" size={18} /></article>)}</div></section><footer><span>APEXNOVA / FREE FIRE + BGMI</span><span><WalletCards size={14} /> ALL PAYMENTS ARE MOCKED</span></footer></main>; }
